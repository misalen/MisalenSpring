<?xml version="1.0"?>
<thlogic>
	<attr sel="#form" th:action="@{/sys/table/info/leadin}">
	</attr>
	<attr sel="#tableName" th:remove="all-but-first">
		<attr sel="option" th:each="menu1 : ${tableName}"
			th:value="${menu1}" th:text="${menu1}">
		</attr>
	</attr>
</thlogic>