<?xml version="1.0"?>
<thlogic>
	<attr sel="#form" th:action="@{/sys/flow/info/update}">
	</attr>
	<attr sel="#primaryKey" th:value="${model.primaryKey}">
	</attr>
	<attr sel="#name" th:value="${model.name}">
	</attr>
</thlogic>