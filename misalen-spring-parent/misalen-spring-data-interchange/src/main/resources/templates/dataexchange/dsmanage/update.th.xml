<?xml version="1.0"?>
<thlogic>
	<attr sel="#form" th:action="@{/dataexchange/dsmanage/update}">
	</attr>
	<attr sel="#primaryKey" th:value="${model.primaryKey}">
	</attr>
	<attr sel="#dsname" th:value="${model.dsname}">
	</attr>
	<attr sel="#leixing" th:value="${model.leixing}">
	</attr>
	<attr sel="#iPdizhi" th:value="${model.iPdizhi}">
	</attr>
	<attr sel="#port" th:value="${model.port}">
	</attr>
	<attr sel="#username" th:value="${model.username}">
	</attr>
	<attr sel="#password" th:value="${model.password}">
	</attr>
</thlogic>