<?xml version="1.0"?>
<thlogic>
	<attr sel="#form" th:action="@{/sys/role/update}">
	</attr>
	<attr sel="#primaryKey" th:value="${model.primaryKey}">
	</attr>
	<attr sel="#text" th:value="${model.text}">
	</attr>
	<attr sel="#desc" th:value="${model.desc}">
	</attr>
	<attr sel="#code" th:value="${model.code}">
	</attr>
</thlogic>