<?xml version="1.0"?>
<thlogic>
	<attr sel="#form" th:action="@{/sys/admin/update}">
	</attr>
	<attr sel="#primaryKey" th:value="${model.primaryKey}">
	</attr>
	<attr sel="#username" th:value="${model.username}">
	</attr>
	<attr sel="#nickname" th:value="${model.nickname}">
	</attr>
</thlogic>